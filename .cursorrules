# Cursor Rules

This repository follows the canonical local agent rules in `AGENTS.md`.

For design and structure guidance, agents should first load:
- https://github.com/EqualExperts/kuat-agent-rules

Then apply local implementation rules from:
- `AGENTS.md`
